# Dedicated lmm-api cache. Credentials live in the separately protected ACL file.
bind 127.0.0.1 -::1
protected-mode yes
port 6380
tcp-backlog 128
timeout 0
tcp-keepalive 300

supervised systemd
daemonize no
pidfile /run/valkey-lmm-api/valkey-lmm-api.pid
loglevel notice
logfile ""

databases 1
dir /var/lib/valkey-lmm-api
dbfilename dump.rdb
aclfile /etc/valkey/lmm-api.acl

# Security fences and rate-limit counters must never be evicted. At the memory
# limit, reject writes so callers can fail closed instead of losing state.
maxmemory 64mb
maxmemory-policy noeviction
maxmemory-samples 5

# Retain useful warm state across a restart without treating it as authoritative.
save ""
appendonly yes
appendfilename "appendonly.aof"
appenddirname "appendonlydir"
appendfsync everysec
no-appendfsync-on-rewrite yes
aof-use-rdb-preamble yes
